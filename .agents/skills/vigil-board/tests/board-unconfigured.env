# Fixture reproducing an unbootstrapped board.env: the repository is known,
# but the board itself has not been created yet, so VIGIL_BOARD_PROJECT_ID is
# still a TODO(bootstrap) placeholder. Used by helpers-regression.sh to prove
# every helper fails clearly instead of guessing.
VIGIL_BOARD_OWNER=ceponatia
VIGIL_BOARD_REPO=ceponatia/vigil-fixture
VIGIL_BOARD_NUMBER="TODO(bootstrap): set VIGIL_BOARD_NUMBER to the project number once the board is created"
VIGIL_BOARD_PROJECT_ID="TODO(bootstrap): set VIGIL_BOARD_PROJECT_ID to the project's GraphQL node id once the board is created"
VIGIL_BOARD_ASSIGNEE=ceponatia
