# Fixture reproducing an unbootstrapped review.env: the repository itself is
# not yet known. Used to prove every helper fails clearly, before any gh
# call, instead of guessing.
VIGIL_REVIEW_OWNER=ceponatia
VIGIL_REVIEW_REPO="TODO(bootstrap): set VIGIL_REVIEW_REPO to owner/name once the repository exists"
VIGIL_REVIEWER_LOGIN="TODO(bootstrap): set VIGIL_REVIEWER_LOGIN once a reviewer identity is designated"
