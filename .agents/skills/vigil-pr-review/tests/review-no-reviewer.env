# Fixture with a known repository but no designated reviewer yet — proves
# review-status.sh degrades only its final classification line instead of
# failing the whole helper.
VIGIL_REVIEW_OWNER=ceponatia
VIGIL_REVIEW_REPO=ceponatia/vigil-fixture
VIGIL_REVIEWER_LOGIN="TODO(bootstrap): set VIGIL_REVIEWER_LOGIN once a reviewer identity is designated"
