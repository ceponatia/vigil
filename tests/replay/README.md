Deterministic, full-lifecycle replays that drive a scenario end to end
through the paper adapter — from a research candidate to a journal entry —
asserting on the final state rather than on each intermediate step; see
`tests/README.md` for naming and how the root Vitest config selects these.
