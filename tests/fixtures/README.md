Synthetic test data only — deterministic and reproducible, and never
containing personal holdings, real wallet or exchange addresses, or keys of
any kind; see `tests/README.md` for how these fixtures are consumed.
