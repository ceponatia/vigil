// Placeholder: @vigil/adapter-paper is implemented under planning ID BOOT-06. This file
// exists so `tsc -p` has an input and the package's `exports` entry
// resolves; it has no runtime behavior.
export {};
